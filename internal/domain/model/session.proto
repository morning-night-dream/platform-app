syntax = "proto3";

// buf:lint:ignore PACKAGE_VERSION_SUFFIX
package model;

option go_package = "github.com/morning-night-dream/platform-app/internal/domain;model";

message Session {
  string session_id = 1;
  string user_id = 2;
  string public_key = 3;
}
